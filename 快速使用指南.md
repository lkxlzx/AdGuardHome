# DNS路由过滤优先级修复 - 快速使用指南

## 问题
DNS路由规则优先级过高，导致广告拦截功能对DNS路由中的域名失效。

## 解决方案
已修复过滤规则优先级，现在广告拦截规则优先于DNS路由规则。

## 快速开始

### 1. 停止当前AdGuardHome
```powershell
Stop-Process -Name "AdGuardHome*" -Force -ErrorAction SilentlyContinue
```

### 2. 启动修复版本
```powershell
.\AdGuardHome_filter_priority_fix.exe
```

### 3. 测试验证
```powershell
# 自动化测试
.\test_dns_routing_filter_priority.ps1

# 或手动测试
nslookup ad.example.cn 127.0.0.1  # 应该被拦截
nslookup baidu.com 127.0.0.1      # 应该走DNS路由
```

## 新的优先级顺序

```
1. 白名单规则 ← 最高优先级
   ↓
2. 黑名单规则 ← 广告拦截等（现在优先于DNS路由）
   ↓
3. DNS路由规则 ← 最低优先级
```

## 效果对比

### 修复前 ❌
```
域名: ad.example.cn
- 在CN域名列表（DNS路由）
- 在广告拦截列表（黑名单）
结果: 不被拦截，走DNS路由
```

### 修复后 ✓
```
域名: ad.example.cn
- 在CN域名列表（DNS路由）
- 在广告拦截列表（黑名单）
结果: 被广告拦截规则拦截
```

## 特殊需求

如果需要某些域名**绕过过滤**并**强制路由**：

1. 添加到白名单：`@@||example.cn^`
2. 保留在DNS路由规则中
3. 结果：不被拦截，使用DNS路由

## 常见问题

**Q: DNS路由不工作了？**  
A: 检查域名是否在黑名单中。如需强制路由，添加到白名单。

**Q: 广告还是不被拦截？**  
A: 确保启用了Protection和Filtering功能。

**Q: 如何查看匹配的规则？**  
A: 查看AdGuardHome的查询日志。

## 文件说明

- `AdGuardHome_filter_priority_fix.exe` - 修复版本
- `test_dns_routing_filter_priority.ps1` - 测试脚本
- `DNS路由过滤优先级修复说明.md` - 详细说明
- `PRIORITY_FIX_SUMMARY.md` - 技术总结

## 验证成功标志

- ✓ 广告域名被拦截（即使在DNS路由规则中）
- ✓ 正常域名走DNS路由
- ✓ 白名单域名不被拦截
- ✓ 查询日志显示正确的过滤原因

## 需要帮助？

查看详细文档：
- 中文：`DNS路由过滤优先级修复说明.md`
- 英文：`DNS_ROUTING_FILTER_PRIORITY_FIX.md`
- 验证：`FILTER_PRIORITY_FIX_VERIFICATION.md`

---

**修复完成时间**: 2025-11-28  
**修复版本**: AdGuardHome_filter_priority_fix.exe  
**状态**: ✓ 编译成功，可以使用
