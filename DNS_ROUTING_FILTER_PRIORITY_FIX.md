# DNS路由过滤优先级修复

## 问题描述

DNS路由功能使用了白名单模板，导致当启用过滤功能（如广告拦截）时，DNS路由规则的优先级高于黑名单过滤规则，使得广告拦截功能对DNS路由规则中的域名失效。

### 问题表现

- 加载了CN域名规则作为DNS路由规则
- 启用了广告拦截过滤功能
- 结果：CN域名列表中的广告域名不会被拦截，因为DNS路由规则（基于白名单模板）优先级更高

## 根本原因

在 `internal/filtering/filtering.go` 的 `matchHost` 函数中，过滤规则的检查顺序为：

1. **DNS路由规则**（最高优先级）
2. 白名单规则
3. 黑名单规则（最低优先级）

这导致DNS路由规则会覆盖广告拦截规则。

## 修复方案

调整过滤规则的检查顺序，确保广告拦截等过滤规则优先于DNS路由规则：

### 新的优先级顺序

1. **白名单规则**（最高优先级 - 明确允许的域名）
2. **黑名单过滤规则**（广告拦截、恶意软件拦截等）
3. **DNS路由规则**（最低优先级 - 仅在未被拦截时生效）

### 修改内容

修改了 `internal/filtering/filtering.go` 中的 `matchHost` 函数：

```go
// 修改前的逻辑：
// 1. 检查DNS路由 -> 直接返回
// 2. 检查白名单 -> 返回
// 3. 检查黑名单 -> 返回

// 修改后的逻辑：
// 1. 检查白名单 -> 返回（明确允许）
// 2. 检查黑名单 -> 返回（拦截优先）
// 3. 检查DNS路由 -> 返回（仅在未被拦截时）
```

### 关键改进

1. **过滤规则优先**：广告拦截、恶意软件拦截等过滤规则现在优先于DNS路由规则
2. **保持DNS路由独立性**：DNS路由仍然独立于 `ProtectionEnabled` 设置
3. **白名单仍然最高**：明确的白名单规则仍然具有最高优先级
4. **FilteringEnabled控制**：当 `FilteringEnabled=false` 时，DNS路由仍然工作

## 行为变化

### 修复前

```
域名: ad.example.cn
- 在CN域名列表中（DNS路由规则）
- 在广告拦截列表中（黑名单规则）
结果: 使用DNS路由，不被拦截 ❌
```

### 修复后

```
域名: ad.example.cn
- 在CN域名列表中（DNS路由规则）
- 在广告拦截列表中（黑名单规则）
结果: 被广告拦截规则拦截 ✓
```

## 测试方法

使用提供的测试脚本验证修复：

```powershell
.\test_dns_routing_filter_priority.ps1
```

### 测试场景

1. **广告域名** (ad.example.cn)
   - 同时在DNS路由规则和广告拦截列表中
   - 预期：被拦截

2. **追踪域名** (tracker.example.cn)
   - 同时在DNS路由规则和追踪拦截列表中
   - 预期：被拦截

3. **正常域名** (normal.example.cn)
   - 仅在DNS路由规则中
   - 预期：走DNS路由，不被拦截

## 兼容性

### 不影响的功能

- ✓ 白名单规则仍然具有最高优先级
- ✓ DNS路由在 `FilteringEnabled=false` 时仍然工作
- ✓ DNS路由独立于 `ProtectionEnabled` 设置
- ✓ 现有的过滤规则配置无需修改

### 影响的行为

- ⚠️ DNS路由规则中的域名现在会受到过滤规则的影响
- ⚠️ 如果某个域名同时在DNS路由规则和黑名单中，将被拦截而不是路由

## 建议

如果需要某些域名绕过过滤规则并强制使用DNS路由，应该：

1. 将这些域名添加到**白名单**中
2. 同时保留在DNS路由规则中
3. 这样白名单会优先生效，然后DNS路由规则会处理路由

## 构建和部署

```powershell
# 构建修复后的版本
.\build-with-frontend.ps1

# 测试
.\test_dns_routing_filter_priority.ps1
```

## 相关文件

- `internal/filtering/filtering.go` - 主要修改文件
- `internal/filtering/result.go` - 过滤原因定义
- `test_dns_routing_filter_priority.ps1` - 测试脚本

## 总结

此修复确保了广告拦截等过滤功能能够正确作用于DNS路由规则中的域名，同时保持了DNS路由功能的独立性和灵活性。用户现在可以同时使用DNS路由和过滤功能，而不会出现过滤失效的问题。
