# 版本更新说明

## 版本信息

- **更新日期**：2025年11月24日
- **主要改动**：Upstream Groups 格式统一

## 重要变更

### 1. 数据格式变更 ⚠️

`upstream_groups` 中的 `upstreams` 字段从字符串格式改为数组格式：

**旧格式**（不再支持）：
```yaml
upstream_groups:
  - id: group_xxx
    name: 国内DNS
    upstreams: |
      114.114.114.114
      223.5.5.5
```

**新格式**（推荐）：
```yaml
upstream_groups:
  - id: group_xxx
    name: 国内DNS
    upstreams:
      - 114.114.114.114
      - 223.5.5.5
```

### 2. 格式统一

现在 `upstreams` 和 `bootstrap_dns` 使用相同的数组格式，更加直观和一致。

### 3. API 超时优化

- DNS 配置更新请求超时：30 秒
- 其他 API 请求超时：10 秒
- 避免前端长时间等待无响应

## 升级指南

### 自动升级（推荐）

1. 停止旧版本 AdGuard Home
2. 备份 `AdGuardHome.yaml`
3. 替换 `AdGuardHome.exe`
4. 启动新版本
5. 程序会自动处理配置格式

### 手动升级

如果自动升级失败，请手动修改 `AdGuardHome.yaml`：

1. 找到 `upstream_groups` 部分
2. 将每个组的 `upstreams` 改为数组格式
3. 保存文件
4. 启动程序

**示例修改**：

```yaml
# 修改前
upstreams: |
  114.114.114.114
  223.5.5.5

# 修改后
upstreams:
  - 114.114.114.114
  - 223.5.5.5
```

## 功能改进

### 前端

- ✅ 自动转换字符串和数组格式
- ✅ 表单输入保持不变（每行一个地址）
- ✅ 添加 API 超时避免卡死
- ✅ 改进错误处理

### 后端

- ✅ 统一数据结构（`[]string`）
- ✅ 简化处理逻辑
- ✅ 提高代码可维护性
- ✅ 与其他配置项保持一致

## 兼容性

- ✅ 向后兼容：可以读取旧格式配置
- ✅ 自动转换：保存时自动转为新格式
- ✅ 前端兼容：用户界面保持不变

## 已知问题

无

## 测试建议

1. 备份配置文件
2. 启动新版本
3. 检查上游组是否正常显示
4. 尝试添加/编辑/删除上游组
5. 验证 DNS 解析是否正常

## 回滚方案

如果遇到问题，可以回滚到旧版本：

1. 停止新版本
2. 恢复备份的 `AdGuardHome.yaml`
3. 使用旧版本 `AdGuardHome.exe`
4. 启动程序

## 技术细节

详细的技术说明请参考：
- `UPSTREAM_GROUPS_FIX.md` - 完整修复说明
- `QUICK_FIX_SUMMARY.md` - 快速总结
- `BUILD_INSTRUCTIONS.md` - 编译说明

## 反馈

如果遇到问题，请提供：
1. 错误信息
2. `AdGuardHome.yaml` 配置（去除敏感信息）
3. 浏览器控制台日志
4. 操作步骤
