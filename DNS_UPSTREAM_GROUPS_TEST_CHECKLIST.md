# DNS 上游服务器分组功能测试清单

## 一、基础功能测试

### 1.1 添加分组
- [ ] 点击"添加分组"按钮，显示添加表单
- [ ] 输入分组名称和 DNS 服务器列表
- [ ] 点击"保存"按钮，成功添加分组
- [ ] **第一个添加的分组自动设为默认组**
- [ ] 添加后表单关闭，显示新分组
- [ ] 点击"取消"按钮，表单关闭，不添加分组

### 1.2 编辑分组
- [ ] 点击分组的"编辑"按钮，原地展开编辑表单
- [ ] 表单预填充当前分组的名称和服务器列表
- [ ] 修改内容后点击"保存"，成功更新分组
- [ ] **编辑时保留 isDefault 状态**
- [ ] 点击"取消"按钮，表单关闭，不修改分组

### 1.3 删除分组
- [ ] 点击非默认组的"删除"按钮，弹出确认对话框
- [ ] 确认后成功删除分组
- [ ] **删除后如果没有默认组，自动将第一个分组设为默认**
- [ ] 点击默认组的"删除"按钮，按钮为禁用状态
- [ ] 尝试删除默认组时，弹出提示"无法删除默认组，请先设置其他组为默认组"

### 1.4 设置默认组
- [ ] 非默认组显示"设为默认"按钮
- [ ] 点击"设为默认"按钮，该组变为默认组
- [ ] **原默认组自动取消默认状态**
- [ ] **同一时间只有一个默认组**
- [ ] 默认组不显示"设为默认"按钮

## 二、UI 显示测试

### 2.1 默认组标识
- [ ] 默认组使用黄色边框（2px）
- [ ] 默认组使用黄色背景（#fff3cd）
- [ ] 默认组名称旁显示蓝色"默认组"徽章
- [ ] 默认组下方显示提示文字"分流规则未命中时使用此组"

### 2.2 空状态
- [ ] 没有分组时显示空状态提示
- [ ] 空状态提示文字："暂无分组，点击下方按钮添加第一个分组（将自动设为默认组）"
- [ ] 空状态使用虚线边框

### 2.3 按钮状态
- [ ] 编辑某个分组时，其他分组的按钮禁用
- [ ] 添加分组时，所有现有分组的按钮禁用
- [ ] 默认组的删除按钮始终禁用
- [ ] 保存配置时（processingSetConfig），所有按钮禁用

### 2.4 表单显示
- [ ] 添加表单使用蓝色边框高亮
- [ ] 编辑表单在原分组位置展开
- [ ] 表单字段标签清晰
- [ ] 输入框有合适的占位符文本

## 三、数据验证测试

### 3.1 必填字段
- [ ] 分组名称为空时无法保存
- [ ] DNS 服务器列表为空时无法保存
- [ ] 输入前后空格会被自动去除

### 3.2 数据格式
- [ ] DNS 服务器列表支持多行输入
- [ ] 每行一个 DNS 服务器地址
- [ ] 保存后正确显示在预览区域

## 四、持久化测试

### 4.1 配置保存
- [ ] 添加分组后自动保存到后端
- [ ] 编辑分组后自动保存到后端
- [ ] 删除分组后自动保存到后端
- [ ] 设置默认组后自动保存到后端

### 4.2 配置加载
- [ ] 页面刷新后分组数据正确加载
- [ ] 默认组标识正确恢复
- [ ] 分组顺序保持一致

## 五、集成测试

### 5.1 与 DNS 设置表单集成
- [ ] 分组管理区域在 DNS 设置表单下方
- [ ] 使用分隔线清晰区分
- [ ] 不影响现有 DNS 设置功能

### 5.2 与 DNS 分流模块集成
- [ ] DNS 分流规则可以选择自定义分组
- [ ] 分流规则下拉框显示所有分组名称
- [ ] 选择分组后正确应用到 DNS 查询
- [ ] **分流规则未命中时使用默认组**
- [ ] **没有默认组时回退到系统默认上游**

## 六、边界情况测试

### 6.1 极端情况
- [ ] 只有一个分组时的行为（必须是默认组，不可删除）
- [ ] 大量分组时的显示和性能
- [ ] 分组名称很长时的显示
- [ ] DNS 服务器列表很长时的显示

### 6.2 并发操作
- [ ] 快速连续添加多个分组
- [ ] 快速切换默认组
- [ ] 编辑过程中切换到其他页面

## 七、错误处理测试

### 7.1 网络错误
- [ ] 保存失败时的错误提示
- [ ] 加载失败时的错误提示
- [ ] 网络超时时的处理

### 7.2 数据错误
- [ ] 后端返回无效数据时的处理
- [ ] 分组 ID 冲突时的处理
- [ ] 默认组标识丢失时的自动修复

## 八、响应式测试

### 8.1 不同屏幕尺寸
- [ ] 桌面端（>1200px）显示正常
- [ ] 平板端（768px-1200px）显示正常
- [ ] 移动端（<768px）显示正常
- [ ] 按钮在小屏幕上不重叠

### 8.2 浏览器兼容性
- [ ] Chrome 最新版
- [ ] Firefox 最新版
- [ ] Safari 最新版
- [ ] Edge 最新版

## 九、性能测试

### 9.1 渲染性能
- [ ] 10 个分组时渲染流畅
- [ ] 50 个分组时渲染流畅
- [ ] 100 个分组时渲染性能可接受

### 9.2 操作响应
- [ ] 点击按钮响应及时
- [ ] 表单展开/收起动画流畅
- [ ] 保存操作响应及时

## 十、国际化测试

### 10.1 中文界面
- [ ] 所有文本正确显示中文
- [ ] 提示信息使用中文
- [ ] 按钮文字使用中文

### 10.2 英文界面
- [ ] 所有文本正确显示英文
- [ ] 提示信息使用英文
- [ ] 按钮文字使用英文

## 测试结果记录

| 测试项 | 状态 | 备注 | 测试人 | 测试日期 |
|--------|------|------|--------|----------|
|        |      |      |        |          |

**状态说明**：
- ✅ 通过
- ❌ 失败
- ⚠️ 部分通过
- ⏸️ 待测试
