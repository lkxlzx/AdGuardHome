# 🌐 浏览器测试指南

## 🚀 快速开始

### 1. 访问 Web 界面
打开浏览器，访问：
```
http://127.0.0.1
```

### 2. 登录
- **用户名**: `lkxlzx`
- **密码**: （你设置的密码）

### 3. 进入 DNS 设置
1. 点击左侧菜单的"设置"
2. 点击"DNS 设置"
3. 向下滚动找到"上游 DNS 服务器"卡片

## 📋 测试步骤

### 测试 1: 查看 UI
**目标**: 确认 UI 正确显示

**步骤**:
1. 找到"上游 DNS 服务器"卡片
2. 检查是否有表格显示
3. 检查表格列：
   - ✅ 已启用（单选框列）
   - ✅ 分组名称
   - ✅ 上游服务器地址
   - ✅ 操作（编辑和删除按钮）
4. 检查底部是否有"添加DNS上游分组"按钮

**预期结果**:
- 表格正确显示（可能为空）
- 显示"未找到上游DNS分组"（如果没有数据）
- "添加DNS上游分组"按钮存在且可点击

---

### 测试 2: 添加第一个分组
**目标**: 测试添加功能，第一个分组应自动设为默认

**步骤**:
1. 点击"添加DNS上游分组"按钮
2. 在模态框中填写：
   - **分组名称**: `国内 DNS`
   - **上游服务器列表**:
     ```
     223.5.5.5
     119.29.29.29
     https://dns.alidns.com/dns-query
     ```
3. 点击"保存"按钮

**预期结果**:
- ✅ 模态框关闭
- ✅ 表格中显示新分组
- ✅ "已启用"列的单选框被选中（默认组）
- ✅ 分组名称显示为"国内 DNS"
- ✅ 上游服务器地址显示为逗号分隔的列表

---

### 测试 3: 添加第二个分组
**目标**: 测试添加多个分组

**步骤**:
1. 再次点击"添加DNS上游分组"按钮
2. 填写：
   - **分组名称**: `国外 DNS`
   - **上游服务器列表**:
     ```
     8.8.8.8
     1.1.1.1
     https://dns.google/dns-query
     ```
3. 点击"保存"

**预期结果**:
- ✅ 表格中显示两个分组
- ✅ 第一个分组仍然是默认组（单选框选中）
- ✅ 第二个分组的单选框未选中

---

### 测试 4: 编辑分组
**目标**: 测试编辑功能

**步骤**:
1. 找到"国内 DNS"分组
2. 点击"编辑"按钮（铅笔图标）
3. 修改分组名称为：`国内 DNS（阿里云）`
4. 点击"保存"

**预期结果**:
- ✅ 模态框显示当前数据
- ✅ 保存后表格更新
- ✅ 分组名称变为"国内 DNS（阿里云）"

---

### 测试 5: 切换默认组
**目标**: 测试设置默认组功能

**步骤**:
1. 点击"国外 DNS"分组的单选框
2. 观察变化

**预期结果**:
- ✅ "国外 DNS"的单选框被选中
- ✅ "国内 DNS（阿里云）"的单选框取消选中
- ✅ 只有一个分组是默认组

---

### 测试 6: 尝试删除默认组
**目标**: 测试默认组保护

**步骤**:
1. 找到当前默认组（"国外 DNS"）
2. 点击"删除"按钮（垃圾桶图标）

**预期结果**:
- ✅ 删除按钮应该是禁用状态（灰色）
- ✅ 或者显示提示："无法删除默认组，请先设置其他组为默认组"

---

### 测试 7: 删除非默认组
**目标**: 测试删除功能

**步骤**:
1. 先将"国内 DNS（阿里云）"设为默认组
2. 点击"国外 DNS"的删除按钮
3. 在确认对话框中点击"确定"

**预期结果**:
- ✅ 显示确认对话框："确定要删除分组 \"国外 DNS\" 吗？"
- ✅ 确认后分组被删除
- ✅ 表格中只剩下"国内 DNS（阿里云）"

---

### 测试 8: 表单验证
**目标**: 测试表单验证功能

**步骤**:
1. 点击"添加DNS上游分组"
2. 不填写任何内容，直接点击"保存"

**预期结果**:
- ✅ 显示错误提示："必填字段"
- ✅ 分组名称和服务器列表都显示错误
- ✅ 模态框不关闭

**步骤 2**:
1. 只填写分组名称："测试"
2. 不填写服务器列表
3. 点击"保存"

**预期结果**:
- ✅ 服务器列表显示错误："必填字段"
- ✅ 模态框不关闭

---

### 测试 9: 数据持久化
**目标**: 测试数据保存和恢复

**步骤 1 - 刷新页面**:
1. 添加几个分组
2. 按 F5 刷新页面
3. 重新进入 DNS 设置

**预期结果**:
- ✅ 所有分组仍然存在
- ✅ 默认组设置保持不变

**步骤 2 - 重启服务器**:
1. 停止 AdGuard Home
2. 重新启动
3. 登录并查看 DNS 设置

**预期结果**:
- ✅ 所有分组仍然存在
- ✅ 默认组设置保持不变

---

### 测试 10: 分页功能
**目标**: 测试分页（如果有超过 10 个分组）

**步骤**:
1. 添加 15 个分组
2. 观察分页控制

**预期结果**:
- ✅ 表格显示前 10 个分组
- ✅ 底部显示分页控制："上一页 | 页 1 / 2 | 10 行 ▼ | 下一页"
- ✅ 点击"下一页"显示剩余分组
- ✅ 可以更改页面大小（10/25/50/100）

---

### 测试 11: 国际化
**目标**: 测试中英文切换

**步骤**:
1. 在设置中切换语言为"English"
2. 查看上游分组界面

**预期结果**:
- ✅ 所有文本变为英文
- ✅ 按钮文本："Add DNS Upstream Group"
- ✅ 表格列标题为英文

**步骤 2**:
1. 切换回中文
2. 查看界面

**预期结果**:
- ✅ 所有文本恢复中文

---

## 📊 测试检查清单

### UI 显示
- [ ] 表格正确显示
- [ ] 列标题正确
- [ ] 按钮存在且可点击
- [ ] 空状态显示正确

### 添加功能
- [ ] 模态框正确弹出
- [ ] 表单字段正确显示
- [ ] 保存后数据显示在表格中
- [ ] 第一个分组自动设为默认

### 编辑功能
- [ ] 编辑按钮可点击
- [ ] 模态框显示当前数据
- [ ] 修改后数据正确更新

### 删除功能
- [ ] 删除按钮可点击
- [ ] 显示确认对话框
- [ ] 默认组不能删除
- [ ] 非默认组可以删除

### 默认组管理
- [ ] 单选框正确显示
- [ ] 可以切换默认组
- [ ] 只有一个默认组
- [ ] 默认组有视觉标识

### 表单验证
- [ ] 空字段显示错误
- [ ] 错误提示清晰
- [ ] 验证通过后可保存

### 数据持久化
- [ ] 刷新页面后数据保留
- [ ] 重启服务器后数据恢复
- [ ] 配置文件正确保存

### 分页功能
- [ ] 分页控制显示
- [ ] 上一页/下一页工作
- [ ] 页面大小选择工作

### 国际化
- [ ] 中文显示正确
- [ ] 英文显示正确
- [ ] 切换语言正常

## 🎯 测试重点

### 必须测试
1. ✅ 添加分组
2. ✅ 编辑分组
3. ✅ 删除分组
4. ✅ 设置默认组
5. ✅ 数据持久化

### 建议测试
6. ✅ 表单验证
7. ✅ 默认组保护
8. ✅ 页面刷新
9. ✅ 服务器重启

### 可选测试
10. ⏸️ 分页功能（需要多个分组）
11. ⏸️ 国际化
12. ⏸️ 响应式布局

## 🐛 如何报告问题

如果发现问题，请记录：
1. **问题描述**: 发生了什么
2. **重现步骤**: 如何重现
3. **预期结果**: 应该发生什么
4. **实际结果**: 实际发生了什么
5. **截图**: 如果可能，提供截图
6. **浏览器**: 使用的浏览器和版本
7. **控制台**: 浏览器控制台的错误信息

## 📝 测试记录模板

```
测试项目: [测试名称]
测试时间: [日期时间]
测试结果: [通过/失败]
问题描述: [如果失败，描述问题]
截图: [如果有]
```

## 🎊 测试完成

完成所有测试后，请确认：
- [ ] 所有核心功能正常工作
- [ ] 没有发现严重问题
- [ ] 数据持久化正常
- [ ] UI 显示正确

如果所有测试通过，功能就可以投入使用了！🎉

---

**测试指南版本**: 1.0  
**创建日期**: 2024年11月24日  
**服务器地址**: http://127.0.0.1
