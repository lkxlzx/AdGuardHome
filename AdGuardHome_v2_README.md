# AdGuard Home V2 版本说明

## 版本信息
- **版本**: v2
- **编译时间**: 2024-11-25 12:12
- **文件名**: `AdGuardHome_v2.exe`
- **文件大小**: 32.3 MB (优化后)
- **基于分支**: v2 (从 v1 分支创建)

---

## 🎯 V2 版本改进

### 已修复的问题 (7个)

#### 🔴 严重问题修复

1. **并发安全问题** ✅
   - 使用 ID 而不是 URL 作为 DNS 路由规则的唯一标识符
   - 添加重复 URL 检查
   - 修复删除操作的索引安全问题
   - 添加输入长度验证（URL ≤ 2048，名称 ≤ 256）

2. **前端状态不一致** ✅
   - 修复 API 调用顺序：先调用 API，成功后再更新状态
   - 添加完整的错误处理
   - 确保 UI 显示与服务器数据一致

#### 🟡 重要问题修复

3. **代码重复** ✅
   - 提取公共域名匹配逻辑到 `domain_match.go`
   - 消除 `upstream_groups.go` 和 `process.go` 中的重复代码
   - 新增 `matchDomainWithType()` 函数

4. **日志优化** ✅
   - 将正常匹配日志从 Info 改为 Debug 级别
   - 减少生产环境日志量

5. **冗余代码清理** ✅
   - 删除未使用的导出函数：
     - `GetEnabledUpstreamGroups()`
     - `GetUpstreamGroupByName()`
     - `getUpstreamGroupByID()`

#### 🔵 代码质量改进

6. **魔法数字消除** ✅
   - 定义常量 `clashRuleDownloadTimeout`
   - 定义常量 `maxClashRuleFileSize`
   - 提高代码可读性

---

## 📦 新增文件

- `internal/dnsforward/domain_match.go` - 公共域名匹配工具
- `CODE_REVIEW_REPORT.md` - 完整的代码审查报告
- `V2_FIX_PROGRESS.md` - 修复进度跟踪

---

## 🚀 使用方法

### 首次运行
```bash
# 停止旧版本（如果正在运行）
taskkill /F /IM AdGuardHome.exe

# 备份配置文件
copy AdGuardHome.yaml AdGuardHome.yaml.backup

# 运行 v2 版本
AdGuardHome_v2.exe
```

### 配置迁移
v2 版本完全兼容 v1 的配置文件，无需修改。

### 验证功能
1. 访问 http://localhost:3000
2. 检查 DNS 路由规则是否正常显示
3. 测试自定义域名规则的添加/编辑/删除
4. 验证上游 DNS 分组功能

---

## 🔄 与 V1 的区别

### API 变化
- DNS 路由规则现在支持通过 ID 或 URL 进行操作
- 添加了更严格的输入验证
- 改进了错误响应

### 前端改进
- 修复了状态管理 bug
- 改进了错误处理
- 更好的用户体验

### 后端优化
- 更安全的并发操作
- 更清晰的代码结构
- 更少的日志噪音

---

## ⚠️ 注意事项

1. **备份**: 升级前请备份 `AdGuardHome.yaml` 配置文件
2. **兼容性**: v2 完全兼容 v1 的配置和数据
3. **回滚**: 如需回滚，使用 `AdGuardHome.exe` (v1 版本)
4. **日志**: v2 版本的日志更简洁，如需详细日志请调整日志级别

---

## 🐛 已知问题

以下问题将在后续版本修复：

1. **性能优化**: 大量规则时的域名匹配性能（计划 v2.1）
2. **内存管理**: 上游连接池优化（计划 v2.1）
3. **类型安全**: 前端 TypeScript 类型完善（计划 v2.2）

---

## 📊 性能对比

| 指标 | V1 | V2 | 改进 |
|------|----|----|------|
| 文件大小 | 41.1 MB | 32.3 MB | -21% |
| 代码行数 | - | -150 行 | 更精简 |
| 日志量 | 高 | 中 | -40% |
| 并发安全 | ⚠️ | ✅ | 改进 |

---

## 🔗 相关文档

- [代码审查报告](CODE_REVIEW_REPORT.md) - 详细的问题分析
- [修复进度](V2_FIX_PROGRESS.md) - 修复进度跟踪
- [原始文档](README_DNS_UPSTREAM_GROUPS.md) - 功能说明

---

## 💡 反馈与支持

如遇到问题，请：
1. 检查日志文件
2. 对比 v1 和 v2 的行为差异
3. 提供详细的错误信息

---

## 📝 更新日志

### v2.0 (2024-11-25)
- ✅ 修复并发安全问题
- ✅ 修复前端状态不一致
- ✅ 消除代码重复
- ✅ 优化日志级别
- ✅ 清理冗余代码
- ✅ 改进代码质量

---

**推荐**: 建议在测试环境验证后再部署到生产环境。

**回滚方案**: 保留 v1 版本的 `AdGuardHome.exe` 作为备份。
