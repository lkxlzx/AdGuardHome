# 域名路由缓存配置说明

## 概述

V3版本新增了域名路由缓存配置选项，允许用户自定义LRU缓存的容量。

## 配置方法

在 `AdGuardHome.yaml` 的 `dns` 部分添加：

```yaml
dns:
  # ... 其他DNS配置 ...
  domain_cache_size: 1000  # 域名路由缓存容量
```

## 配置说明

### domain_cache_size

- **类型**: 整数
- **默认值**: 1000
- **有效范围**: 任意正整数（建议 1000-10000）
- **说明**: 
  - 设置域名路由查找结果的LRU缓存容量
  - 如果设置为 0 或负数，将使用默认值 1000
  - 缓存会存储最近查询的域名及其匹配的上游组
  - 缓存命中时，查询性能提升约 5 倍

## 性能影响

### 缓存容量建议

| 使用场景 | 建议容量 | 说明 |
|---------|---------|------|
| 家庭用户 | 1000 | 默认值，适合大多数场景 |
| 小型办公室 | 2000-3000 | 更多设备和域名 |
| 中型企业 | 5000-10000 | 大量设备和复杂路由规则 |
| 高负载服务器 | 10000+ | 需要更大的缓存容量 |

### 内存占用估算

每个缓存条目约占用 100-200 字节内存：

- 1000 条目 ≈ 100-200 KB
- 5000 条目 ≈ 500 KB - 1 MB
- 10000 条目 ≈ 1-2 MB

## 缓存行为

### 缓存内容

缓存存储以下映射关系：
- 域名 → 上游组ID
- 包括正向匹配（找到规则）和负向匹配（未找到规则）

### 缓存失效

缓存在以下情况会被清空：
- DNS路由规则更新时
- 自定义域名规则更新时
- 手动调用 `ClearDomainCache()` 方法

### LRU淘汰策略

当缓存达到容量上限时：
- 自动淘汰最久未使用的条目
- 保留最近访问的热点域名

## 监控和调试

### 查看缓存统计

可以通过 `GetDomainCacheStats()` 方法获取缓存统计信息：

```go
stats := server.GetDomainCacheStats()
// stats.Size - 当前缓存条目数
// stats.Capacity - 缓存容量
```

### 日志输出

启用 Debug 日志级别可以看到：
- 缓存初始化信息
- 缓存命中/未命中
- 缓存清空操作

## 示例配置

### 默认配置（家庭用户）

```yaml
dns:
  domain_cache_size: 1000
```

### 高性能配置（企业用户）

```yaml
dns:
  domain_cache_size: 10000
```

### 禁用缓存

如果需要禁用缓存（不推荐），可以设置为 1：

```yaml
dns:
  domain_cache_size: 1
```

## 技术细节

### 实现文件

- `internal/dnsforward/domain_cache.go` - 缓存实现
- `internal/dnsforward/config.go` - 配置定义
- `internal/dnsforward/dnsforward.go` - 缓存初始化

### 线程安全

缓存使用 `sync.RWMutex` 实现线程安全：
- 读操作使用读锁（允许并发读）
- 写操作使用写锁（独占访问）

### 性能特性

- Get 操作：~195 ns/op
- Set 操作：~209 ns/op
- 内存分配：每次操作仅 1 次分配

## 升级说明

### 从 V2 升级到 V3

V3 版本会自动使用默认值 1000，无需修改配置文件。

如果需要自定义容量，在配置文件中添加 `domain_cache_size` 字段即可。

### 配置兼容性

- ✅ 向后兼容：旧配置文件不包含此字段时使用默认值
- ✅ 热更新：修改配置后重启服务生效

## 常见问题

### Q: 缓存容量设置多大合适？

A: 默认的 1000 适合大多数场景。如果你有大量自定义路由规则和设备，可以增加到 5000-10000。

### Q: 缓存会占用多少内存？

A: 每 1000 条目约占用 100-200 KB，对系统影响很小。

### Q: 如何知道缓存是否生效？

A: 启用 Debug 日志，可以看到 "cache hit" 消息表示缓存命中。

### Q: 缓存会导致规则更新不生效吗？

A: 不会。规则更新时会自动清空缓存，确保使用最新规则。

## 相关文档

- [ISSUE_5_PROGRESS.md](ISSUE_5_PROGRESS.md) - 性能优化详细说明
- [V3_DEVELOPMENT_PLAN.md](V3_DEVELOPMENT_PLAN.md) - V3 开发计划

---

**更新时间**: 2024-11-25  
**版本**: V3
