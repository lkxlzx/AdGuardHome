# Dashboard V2 更新说明

## 🎉 重大更新

全新设计的性能监控区域，提供更直观、更丰富的缓存和预取指标展示！

## ✨ 新功能

### 1. 性能监控区域（置顶显示）

在 Dashboard 页面顶部新增了一个突出的性能监控区域，包含两个主要卡片：

#### 📊 DNS 缓存指标卡片
- **缓存大小**：显示当前缓存占用空间（自动格式化为 B/KB/MB/GB）
- **最小 TTL**：缓存条目的最小生存时间
- **最大 TTL**：缓存条目的最大生存时间
- **乐观缓存**：是否启用乐观缓存模式

#### 🔥 Prefetch 状态卡片
- **运行状态**：实时显示预取功能的运行状态（空闲/运行中）
- **成功率圆环**：可视化展示预取操作的成功率
- **热门域名数**：当前正在预取的热门域名数量
- **跟踪域名数**：系统跟踪的总域名数
- **活跃任务数**：当前正在执行的预取任务数
- **任务统计条**：
  - 已完成任务数（绿色进度条）
  - 失败任务数（红色进度条）
- **队列状态**：显示待处理任务队列的状态

## 🎨 视觉设计

### 渐变背景
- 使用紫色渐变背景（#667eea → #764ba2）突出性能监控区域
- 白色卡片与深色背景形成鲜明对比

### 动画效果
- 卡片悬停时有轻微上浮效果
- 运行状态指示器有脉冲动画
- 进度条有平滑的过渡动画

### 响应式布局
- **桌面端**：两个卡片并排显示（各占 50%）
- **平板端**：两个卡片并排显示（各占 50%）
- **移动端**：两个卡片堆叠显示（各占 100%）

## 🔄 自动刷新

性能监控数据每 5 秒自动刷新一次，确保显示最新的运行状态。

## 📁 文件变更

### 新增文件
1. `client/src/components/Dashboard/PerformanceMonitoring.tsx` - 性能监控组件
2. `client/src/components/Dashboard/PerformanceMonitoring.css` - 样式文件

### 修改文件
1. `client/src/components/Dashboard/index.tsx` - Dashboard 主页面
2. `client/src/__locales/en.json` - 英文翻译（新增 10 个键）
3. `client/src/__locales/zh-cn.json` - 中文翻译（新增 10 个键）

### 删除文件
- 移除了之前的简单版本 `CacheMetrics.tsx` 和 `PrefetchMetrics.tsx`

## 🚀 使用方法

### 1. 启动服务
```bash
./AdGuardHome_dashboard_v2.exe
```

### 2. 访问 Dashboard
1. 打开浏览器访问 AdGuard Home Web 界面
2. 进入 Dashboard 页面
3. 在页面顶部即可看到新的性能监控区域

### 3. 启用功能
如果显示功能已禁用：
1. 进入 **设置 → DNS 设置**
2. 启用 **DNS 缓存** 和/或 **DNS 预取**
3. 保存配置
4. 返回 Dashboard 查看实时指标

## 📊 指标说明

### DNS 缓存指标

| 指标 | 说明 |
|------|------|
| 缓存大小 | DNS 缓存占用的内存空间 |
| 最小 TTL | 缓存条目的最小生存时间（秒） |
| 最大 TTL | 缓存条目的最大生存时间（秒） |
| 乐观缓存 | 是否在缓存过期时仍提供服务 |

### Prefetch 状态指标

| 指标 | 说明 |
|------|------|
| 运行状态 | 空闲：无任务执行<br>运行中：正在执行预取任务 |
| 成功率 | 预取操作的成功百分比 |
| 热门域名 | 当前被标记为热门的域名数量 |
| 跟踪域名 | 系统跟踪的总域名数量 |
| 活跃任务 | 当前正在执行的预取任务数 |
| 已完成 | 成功完成的预取操作总数 |
| 失败 | 失败的预取操作总数 |
| 队列状态 | 待处理任务的数量 |

## 🎯 设计亮点

### 1. 信息层次清晰
- 最重要的指标（成功率）使用圆环图突出显示
- 次要指标使用网格布局整齐排列
- 详细统计使用进度条可视化

### 2. 状态一目了然
- 使用颜色和图标快速传达状态信息
- 绿色 ✓ 表示正常/成功
- 橙色 ⚡ 表示运行中
- 红色表示失败

### 3. 实时更新
- 自动刷新确保数据始终最新
- 动画效果提供视觉反馈

### 4. 用户友好
- 功能禁用时显示清晰的提示信息
- 所有指标都有明确的标签
- 响应式设计适配各种设备

## 🔧 技术实现

### 组件架构
```
PerformanceMonitoring (容器组件)
├── DNS 缓存指标卡片
│   ├── 缓存大小高亮显示
│   └── 配置参数网格
└── Prefetch 状态卡片
    ├── 状态徽章
    ├── 成功率圆环
    ├── 指标网格
    ├── 任务统计条
    └── 队列状态
```

### API 调用
- `GET /control/dashboard_metrics` - 获取缓存和预取的综合指标
- `GET /control/prefetch_status` - 获取详细的预取状态

### 数据刷新
```typescript
useEffect(() => {
    fetchData();
    const interval = setInterval(fetchData, 5000); // 每5秒刷新
    return () => clearInterval(interval);
}, []);
```

## 📱 响应式断点

| 屏幕宽度 | 布局 |
|---------|------|
| > 1200px | 两列并排 |
| 768px - 1200px | 两列并排 |
| < 768px | 单列堆叠 |
| < 480px | 单列堆叠（紧凑模式） |

## 🎨 配色方案

### 主色调
- 渐变背景：`#667eea` → `#764ba2`
- 卡片背景：`#ffffff`
- 主文字：`#262626`
- 次要文字：`#8c8c8c`

### 状态色
- 成功：`#52c41a`（绿色）
- 警告：`#faad14`（橙色）
- 错误：`#f5222d`（红色）
- 信息：`#1890ff`（蓝色）

## 🐛 已知问题

无

## 🔮 未来改进

1. **历史趋势图**：添加 24 小时的缓存命中率趋势图
2. **详细视图**：点击卡片查看更详细的统计信息
3. **告警功能**：成功率过低时显示告警
4. **导出功能**：导出性能数据为 CSV/JSON
5. **对比功能**：显示与上一时段的对比数据

## 📝 版本对比

### V1 版本（简单版）
- 简单的列表展示
- 静态数据
- 无视觉突出

### V2 版本（当前版）
- 渐变背景突出显示
- 圆环图、进度条等可视化元素
- 自动刷新
- 响应式设计
- 动画效果

## 🙏 致谢

感谢您的耐心等待！新版本的性能监控区域提供了更丰富、更直观的数据展示，希望能帮助您更好地了解 AdGuard Home 的运行状态。

## 📞 反馈

如有任何问题或建议，请随时告知！
