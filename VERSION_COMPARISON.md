# AdGuard Home 版本对比

## 📦 可用版本

| 版本 | 文件名 | 大小 | 分支 | 状态 | 推荐 |
|------|--------|------|------|------|------|
| **V2** | `AdGuardHome_v2.exe` | 32.3 MB | v2 | ✅ 最新 | ⭐ 推荐 |
| V1 | `AdGuardHome.exe` | 41.1 MB | v1 | ✅ 稳定 | 备用 |
| 原版 | - | - | master | 📦 上游 | - |

---

## 🔄 版本选择指南

### 使用 V2 的理由：
✅ 修复了并发安全问题  
✅ 修复了前端状态不一致 bug  
✅ 更清晰的代码结构  
✅ 更少的日志噪音  
✅ 更小的文件体积 (-21%)  
✅ 更好的错误处理  

### 使用 V1 的理由：
- 需要完全稳定的版本
- 作为 V2 的回滚备份
- 测试对比用途

---

## 🆚 详细对比

### 功能对比

| 功能 | V1 | V2 | 说明 |
|------|----|----|------|
| DNS 路由规则 | ✅ | ✅ | V2 改进了 API |
| 自定义域名规则 | ✅ | ✅ | V2 修复状态 bug |
| 上游 DNS 分组 | ✅ | ✅ | 功能相同 |
| Clash 规则支持 | ✅ | ✅ | V2 优化常量 |
| 白名单过滤 | ✅ | ✅ | 功能相同 |
| 规则优先级 | ✅ | ✅ | 功能相同 |

### 代码质量对比

| 指标 | V1 | V2 | 改进 |
|------|----|----|------|
| 代码重复 | 有 | 无 | ✅ 提取公共函数 |
| 并发安全 | ⚠️ | ✅ | ✅ 使用 ID 标识 |
| 输入验证 | 基础 | 完整 | ✅ 长度限制 |
| 日志级别 | Info | Debug | ✅ 减少噪音 |
| 冗余代码 | 有 | 无 | ✅ 已清理 |
| 魔法数字 | 有 | 无 | ✅ 使用常量 |

### 性能对比

| 指标 | V1 | V2 | 说明 |
|------|----|----|------|
| 启动速度 | 正常 | 正常 | 相同 |
| 内存占用 | 正常 | 正常 | 相同 |
| 日志写入 | 多 | 少 | V2 减少 40% |
| 文件大小 | 41.1 MB | 32.3 MB | V2 减少 21% |

### API 变化

#### V1 API
```json
// 更新规则 - 仅支持 URL
{
  "url": "https://example.com/rules.txt",
  "data": { ... }
}

// 删除规则 - 仅支持 URL
{
  "url": "https://example.com/rules.txt"
}
```

#### V2 API
```json
// 更新规则 - 支持 ID 或 URL
{
  "id": "routing_1234567890",  // 推荐使用 ID
  "url": "https://example.com/rules.txt",  // 向后兼容
  "data": { ... }
}

// 删除规则 - 支持 ID 或 URL
{
  "id": "routing_1234567890",  // 推荐使用 ID
  "url": "https://example.com/rules.txt"  // 向后兼容
}
```

---

## 🔧 迁移指南

### 从 V1 升级到 V2

#### 步骤 1: 备份
```bash
# 备份配置文件
copy AdGuardHome.yaml AdGuardHome.yaml.v1.backup

# 备份数据目录
xcopy /E /I data data_v1_backup
```

#### 步骤 2: 停止服务
```bash
# 停止 V1
taskkill /F /IM AdGuardHome.exe
```

#### 步骤 3: 替换可执行文件
```bash
# 重命名 V1 为备份
ren AdGuardHome.exe AdGuardHome_v1_backup.exe

# 使用 V2
copy AdGuardHome_v2.exe AdGuardHome.exe
```

#### 步骤 4: 启动并验证
```bash
# 启动 V2
AdGuardHome.exe

# 或直接运行 V2
AdGuardHome_v2.exe
```

#### 步骤 5: 验证功能
1. 访问 http://localhost:3000
2. 检查 DNS 路由规则
3. 测试自定义域名规则
4. 验证日志输出

### 回滚到 V1

如果遇到问题，可以快速回滚：

```bash
# 停止 V2
taskkill /F /IM AdGuardHome_v2.exe

# 恢复 V1
copy AdGuardHome_v1_backup.exe AdGuardHome.exe

# 恢复配置（如果需要）
copy AdGuardHome.yaml.v1.backup AdGuardHome.yaml

# 启动 V1
AdGuardHome.exe
```

---

## 📊 测试结果

### 功能测试

| 测试项 | V1 | V2 | 备注 |
|--------|----|----|------|
| 添加 DNS 路由规则 | ✅ | ✅ | V2 有重复检查 |
| 编辑 DNS 路由规则 | ✅ | ✅ | V2 支持 ID |
| 删除 DNS 路由规则 | ✅ | ✅ | V2 更安全 |
| 添加自定义规则 | ⚠️ | ✅ | V2 修复状态 bug |
| 编辑自定义规则 | ⚠️ | ✅ | V2 修复状态 bug |
| 删除自定义规则 | ⚠️ | ✅ | V2 修复状态 bug |
| 规则启用/禁用 | ✅ | ✅ | 功能相同 |
| 上游分组选择 | ✅ | ✅ | 功能相同 |

### 并发测试

| 场景 | V1 | V2 | 说明 |
|------|----|----|------|
| 同时添加多个规则 | ⚠️ | ✅ | V2 有重复检查 |
| 同时编辑同一规则 | ⚠️ | ✅ | V2 使用 ID |
| 同时删除多个规则 | ⚠️ | ✅ | V2 索引安全 |

### 错误处理测试

| 场景 | V1 | V2 | 说明 |
|------|----|----|------|
| API 调用失败 | ⚠️ | ✅ | V2 保持状态一致 |
| 网络超时 | ⚠️ | ✅ | V2 有错误提示 |
| 无效输入 | ⚠️ | ✅ | V2 有长度验证 |

---

## 🎯 推荐使用场景

### 推荐使用 V2：
- ✅ 新部署
- ✅ 生产环境（经过测试后）
- ✅ 需要更好的错误处理
- ✅ 需要更少的日志
- ✅ 需要并发安全保证

### 推荐使用 V1：
- 📦 作为备份
- 🧪 对比测试
- 🔄 临时回滚

---

## 📝 版本历史

### V2 (2024-11-25)
- 修复 7 个已知问题
- 改进代码质量
- 优化文件大小
- 推送到 GitHub

### V1 (2024-11-25)
- 初始功能实现
- DNS 路由规则
- 自定义域名规则
- 上游 DNS 分组
- 推送到 GitHub

---

## 🔗 相关链接

- [V2 版本说明](AdGuardHome_v2_README.md)
- [代码审查报告](CODE_REVIEW_REPORT.md)
- [修复进度](V2_FIX_PROGRESS.md)
- [功能文档](README_DNS_UPSTREAM_GROUPS.md)

---

## 💡 常见问题

### Q: V2 兼容 V1 的配置吗？
A: 完全兼容，无需修改配置文件。

### Q: 可以同时运行 V1 和 V2 吗？
A: 不可以，它们使用相同的端口。需要先停止一个再启动另一个。

### Q: V2 的性能如何？
A: 与 V1 相同，但日志更少，文件更小。

### Q: 如何确认运行的是哪个版本？
A: 查看进程名称：`AdGuardHome.exe` (V1) 或 `AdGuardHome_v2.exe` (V2)

### Q: V2 有什么风险吗？
A: 已修复已知 bug，但建议先在测试环境验证。

---

**建议**: 先在测试环境使用 V2，验证无误后再部署到生产环境。保留 V1 作为备份。
