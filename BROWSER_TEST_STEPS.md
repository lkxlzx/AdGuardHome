# 浏览器测试步骤

## 程序状态
✅ AdGuard Home 正在运行
- HTTP 服务：http://127.0.0.1:80
- DNS 服务：127.0.0.1:53

## 测试步骤

### 1. 访问管理界面

打开浏览器，访问：
```
http://127.0.0.1:80
```

或者：
```
http://192.168.10.16:80
```

### 2. 登录

- **用户名**：lkxlzx
- **密码**：（你设置的密码）

### 3. 进入 DNS 设置

1. 点击左侧菜单 "设置"
2. 点击 "DNS 设置"
3. 滚动到 "上游 DNS 服务器" 部分

### 4. 检查上游组

查看是否显示以下上游组：

| 组名 | 上游服务器 | 状态 | 默认 |
|------|-----------|------|------|
| 国内DNS | 114.114.114.114<br>223.5.5.5 | 启用 | 是 |
| 海外 | 8.8.8.8 | 启用 | 否 |
| 测试 | 11.1.1.1 | 启用 | 否 |

### 5. 测试编辑功能

1. 点击 "国内DNS" 组的 "编辑" 按钮
2. 检查是否显示：
   ```
   114.114.114.114
   223.5.5.5
   ```
3. 尝试添加一个新的上游服务器：
   ```
   114.114.114.114
   223.5.5.5
   8.8.4.4
   ```
4. 点击 "保存"
5. 观察是否在30秒内完成（不应该卡死）

### 6. 测试添加功能

1. 点击 "添加上游组" 按钮
2. 输入：
   - **组名**：测试组
   - **上游服务器**：
     ```
     1.1.1.1
     1.0.0.1
     ```
3. 勾选 "启用"
4. 点击 "保存"
5. 验证是否成功添加

### 7. 查看保存后的配置

打开 `AdGuardHome.yaml` 文件，检查格式是否正确：

```yaml
upstream_groups:
  - id: group_1763970331409
    name: 国内DNS
    upstreams:
      - 114.114.114.114
      - 223.5.5.5
      - 8.8.4.4
    enabled: true
    is_default: true
  - id: group_xxx
    name: 测试组
    upstreams:
      - 1.1.1.1
      - 1.0.0.1
    enabled: true
    is_default: false
```

### 8. 测试 DNS 解析

打开命令行，测试 DNS 解析：

```bash
nslookup baidu.com 127.0.0.1
nslookup google.com 127.0.0.1
```

应该能够正常解析。

## 预期结果

### ✅ 成功标准

1. 管理界面正常显示
2. 上游组列表正确显示所有组
3. 编辑时显示多行上游服务器（每行一个）
4. 保存配置在30秒内完成
5. 配置文件使用数组格式
6. DNS 解析功能正常

### ❌ 失败情况

1. 前端显示空白
2. 上游组不显示或显示错误
3. 编辑时只显示一个上游服务器
4. 保存时卡死超过30秒
5. 配置文件格式错误
6. DNS 解析失败

## 调试工具

### 浏览器开发者工具

按 F12 打开开发者工具，查看：

1. **Console**：查看 JavaScript 错误
2. **Network**：查看 API 请求
   - 查找 `/control/dns_info` 请求
   - 查看返回的 JSON 数据
   - 检查 `upstream_groups` 字段

### 示例 API 响应

正确的响应应该是：

```json
{
  "upstream_groups": [
    {
      "id": "group_1763970331409",
      "name": "国内DNS",
      "upstreams": ["114.114.114.114", "223.5.5.5"],
      "enabled": true,
      "is_default": true
    }
  ]
}
```

## 问题排查

### 如果上游服务器只显示一个

1. 检查 API 响应中的 `upstreams` 字段
2. 如果是数组，检查前端转换逻辑
3. 如果是字符串，检查后端序列化逻辑

### 如果保存时卡死

1. 查看 Network 标签中的请求状态
2. 检查是否在30秒后超时
3. 查看后端日志是否有错误

### 如果配置文件格式错误

1. 停止程序
2. 手动修改 `AdGuardHome.yaml`
3. 确保使用数组格式
4. 重新启动程序

## 测试完成后

请记录：
1. 哪些功能正常
2. 哪些功能有问题
3. 错误信息（截图或文字）
4. 浏览器控制台日志
5. 后端日志

这些信息将帮助我们进一步优化代码。
