# 🧪 DNS 上游分组功能 - 测试指南

## ✅ 服务已启动

**状态**: 🟢 运行中  
**访问地址**: http://127.0.0.1:3000  
**进程 ID**: 2

## 📋 测试步骤

### 第一步：完成初始设置

1. 在浏览器中打开：**http://127.0.0.1:3000**
2. 你会看到 AdGuard Home 的安装向导
3. 按照向导完成初始设置：
   - 设置管理员用户名和密码
   - 配置监听端口（可以使用默认值）
   - 完成设置

### 第二步：登录系统

1. 使用刚才设置的用户名和密码登录
2. 进入 AdGuard Home 管理界面

### 第三步：访问 DNS 设置

1. 点击左侧菜单的 **"设置"** (Settings)
2. 点击 **"DNS 设置"** (DNS Settings)
3. 滚动到页面底部

### 第四步：查看上游分组功能

在 DNS 设置页面底部，你应该能看到：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

上游 DNS 服务器分组
创建上游 DNS 服务器分组，可在 DNS 分流规则中使用这些分组。
未命中分流规则的查询将使用默认组

[暂无分组，点击下方按钮添加第一个分组（将自动设为默认组）]

[+ 添加分组]
```

## 🎯 功能测试清单

### 测试 1: 添加第一个分组 ✅

**步骤**:
1. 点击 **"添加分组"** 按钮
2. 输入分组名称：`国内 DNS`
3. 输入 DNS 服务器（每行一个）：
   ```
   223.5.5.5
   119.29.29.29
   ```
4. 点击 **"保存"** 按钮

**预期结果**:
- ✅ 分组成功添加
- ✅ 显示黄色边框（默认组标识）
- ✅ 显示蓝色 **"默认组"** 徽章
- ✅ 显示提示文字："分流规则未命中时使用此组"

**实际结果**: ⚠️ 保存会失败（因为后端未实现）

### 测试 2: UI 显示测试 ✅

**可以测试的内容**:
- ✅ 组件是否正确渲染
- ✅ 空状态提示是否显示
- ✅ 按钮样式是否正确
- ✅ 表单布局是否合理
- ✅ 响应式设计（调整浏览器窗口大小）

### 测试 3: 表单验证 ✅

**步骤**:
1. 点击 **"添加分组"**
2. 不输入任何内容，直接点击 **"保存"**

**预期结果**:
- ✅ 无法保存（表单验证阻止）

### 测试 4: 取消操作 ✅

**步骤**:
1. 点击 **"添加分组"**
2. 输入一些内容
3. 点击 **"取消"** 按钮

**预期结果**:
- ✅ 表单关闭
- ✅ 不添加分组

## ⚠️ 已知限制

### 当前版本限制

由于后端功能尚未实现，以下功能**不可用**：

1. ❌ **保存分组数据** - 点击保存会失败
2. ❌ **加载分组数据** - 刷新页面后数据丢失
3. ❌ **DNS 分流功能** - 分流规则不会生效
4. ❌ **默认组应用** - DNS 查询不会使用默认组

### 可以测试的内容

1. ✅ **UI 显示** - 所有界面元素
2. ✅ **交互逻辑** - 按钮点击、表单展开/收起
3. ✅ **表单验证** - 必填字段检查
4. ✅ **视觉效果** - 默认组样式、空状态提示
5. ✅ **响应式布局** - 不同屏幕尺寸

## 🔍 检查要点

### UI 检查

- [ ] 分组管理区域在页面底部显示
- [ ] 有清晰的分隔线与上方内容区分
- [ ] 标题和描述文字清晰可读
- [ ] 空状态提示友好
- [ ] "添加分组"按钮样式正确

### 交互检查

- [ ] 点击"添加分组"按钮，表单正确展开
- [ ] 表单有蓝色边框高亮
- [ ] 输入框有合适的占位符文本
- [ ] "保存"和"取消"按钮可点击
- [ ] 点击"取消"，表单正确关闭

### 样式检查

- [ ] 字体大小合适
- [ ] 颜色搭配协调
- [ ] 间距合理
- [ ] 与现有 AdGuard Home UI 风格一致

## 📸 截图建议

建议截图以下内容用于记录：

1. **空状态** - 没有分组时的显示
2. **添加表单** - 展开的添加分组表单
3. **表单验证** - 必填字段验证提示
4. **响应式** - 移动端/平板端显示效果

## 🐛 问题报告

如果发现问题，请记录：

1. **问题描述** - 具体是什么问题
2. **重现步骤** - 如何触发问题
3. **预期行为** - 应该是什么样
4. **实际行为** - 实际是什么样
5. **截图** - 如果可能，提供截图
6. **浏览器** - 使用的浏览器和版本

## 🔄 重启服务

如果需要重启 AdGuard Home：

### 停止服务
在命令行中按 `Ctrl+C` 或使用：
```bash
# 查找进程
tasklist | findstr AdGuardHome

# 结束进程
taskkill /F /IM AdGuardHome.exe
```

### 启动服务
```bash
.\AdGuardHome.exe
```

## 📊 测试结果记录

| 测试项 | 状态 | 备注 |
|--------|------|------|
| UI 显示 | ⏸️ 待测试 | |
| 空状态提示 | ⏸️ 待测试 | |
| 添加表单 | ⏸️ 待测试 | |
| 表单验证 | ⏸️ 待测试 | |
| 取消操作 | ⏸️ 待测试 | |
| 响应式布局 | ⏸️ 待测试 | |
| 保存功能 | ❌ 预期失败 | 后端未实现 |

**状态说明**:
- ✅ 通过
- ❌ 失败
- ⚠️ 部分通过
- ⏸️ 待测试

## 🎯 下一步

### 如果 UI 测试通过

1. 实现后端功能（参考 `BACKEND_IMPLEMENTATION_NEEDED.md`）
2. 重新编译和测试
3. 进行完整的功能测试

### 如果发现 UI 问题

1. 记录问题详情
2. 修复前端代码
3. 重新编译前端
4. 再次测试

## 📞 相关文档

- `DNS_UPSTREAM_GROUPS_TEST_CHECKLIST.md` - 完整测试清单
- `BACKEND_IMPLEMENTATION_NEEDED.md` - 后端实现说明
- `BUILD_AND_TEST_GUIDE.md` - 构建指南
- `TESTING_STATUS.md` - 项目状态

---

**开始测试**: 打开浏览器访问 http://127.0.0.1:3000 🚀
