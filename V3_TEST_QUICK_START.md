# AdGuard Home V3 测试版快速启动指南

## 🚀 快速开始

### 1. 首次启动

```cmd
AdGuardHome_v3_test.exe
```

程序将自动：
- 创建默认配置文件 `AdGuardHome.yaml`
- 启动Web界面（默认端口：3000）
- 打开浏览器进行初始设置

### 2. 访问Web界面

打开浏览器访问：
```
http://localhost:3000
```

### 3. 初始设置

1. 设置管理员账号和密码
2. 配置DNS服务器监听地址（默认：0.0.0.0:53）
3. 配置Web界面监听地址（默认：0.0.0.0:80）

## 📋 测试V3新功能

### 测试域名匹配修复

#### 1. 创建自定义域名规则

进入 **过滤器 > DNS路由规则 > 自定义域名规则**

添加测试规则：
```
域名: example.com
匹配类型: DOMAIN-SUFFIX
上游组: 选择一个上游组
启用: ✓
```

#### 2. 测试大小写不敏感

尝试访问以下域名，应该都能匹配：
- `example.com`
- `Example.COM`
- `EXAMPLE.com`
- `test.example.com`
- `Test.Example.COM`

#### 3. 测试不同匹配类型

**DOMAIN（精确匹配）**:
```
域名: test.com
匹配类型: DOMAIN
```
- ✅ 匹配: `test.com`
- ❌ 不匹配: `www.test.com`

**DOMAIN-SUFFIX（后缀匹配）**:
```
域名: test.com
匹配类型: DOMAIN-SUFFIX
```
- ✅ 匹配: `test.com`
- ✅ 匹配: `www.test.com`
- ✅ 匹配: `api.www.test.com`

**DOMAIN-KEYWORD（关键词匹配）**:
```
域名: google
匹配类型: DOMAIN-KEYWORD
```
- ✅ 匹配: `google.com`
- ✅ 匹配: `www.google.com`
- ✅ 匹配: `google.co.uk`
- ✅ 匹配: `mygooglesite.com`

### 测试规则优先级

#### 1. 创建多个规则

在 **过滤器 > DNS路由规则** 中添加：

```
规则1:
- 优先级: 10
- 域名: DOMAIN-SUFFIX,example.com
- 上游组: Group A

规则2:
- 优先级: 20
- 域名: DOMAIN-SUFFIX,test.com
- 上游组: Group B
```

#### 2. 验证优先级

- 优先级数字越小，优先级越高
- 规则1（优先级10）会优先于规则2（优先级20）匹配

### 测试自动更新

#### 1. 配置更新间隔

在 **过滤器 > DNS路由规则** 中：
- 设置自动更新间隔（例如：24小时）
- 点击保存

#### 2. 手动更新

点击 **立即更新** 按钮，验证：
- 更新进度显示
- 更新完成提示
- 规则数量变化

## 🔍 查看日志

### 启动时显示详细日志

```cmd
AdGuardHome_v3_test.exe -v
```

### 查看日志文件

日志文件位置：
```
./data/querylog.json  - DNS查询日志
```

### 查看域名匹配日志

在日志中搜索：
```
matched custom domain rule
matched dns routing rule
```

## 🛠️ 高级配置

### 使用自定义配置文件

```cmd
AdGuardHome_v3_test.exe -c custom_config.yaml
```

### 使用自定义工作目录

```cmd
AdGuardHome_v3_test.exe -w /path/to/workdir
```

### 检查配置文件

```cmd
AdGuardHome_v3_test.exe --check-config
```

## 📊 性能监控

### 查看DNS查询统计

访问：**仪表板** 页面

查看：
- 查询总数
- 被拦截的查询
- 平均响应时间

### 监控系统资源

使用任务管理器监控：
- CPU使用率（应该 < 10%）
- 内存使用（应该 < 150MB）

## 🐛 故障排除

### 问题1: 无法启动

**解决方案**:
1. 检查端口是否被占用（53, 80, 3000）
2. 以管理员权限运行
3. 检查防火墙设置

### 问题2: DNS查询不工作

**解决方案**:
1. 检查DNS服务器是否正确配置
2. 验证上游DNS服务器可访问
3. 查看日志文件

### 问题3: Web界面无法访问

**解决方案**:
1. 检查Web服务器端口配置
2. 尝试访问 `http://127.0.0.1:3000`
3. 检查防火墙规则

### 问题4: 规则不生效

**解决方案**:
1. 确认规则已启用
2. 检查规则语法是否正确
3. 查看日志中的匹配信息
4. 验证上游组配置正确

## 📝 测试清单

完成以下测试项目：

### 基本功能
- [ ] 程序正常启动
- [ ] Web界面可访问
- [ ] DNS查询正常工作
- [ ] 日志正常记录

### 域名匹配
- [ ] DOMAIN类型匹配正常
- [ ] DOMAIN-SUFFIX类型匹配正常
- [ ] DOMAIN-KEYWORD类型匹配正常
- [ ] 大小写不敏感匹配正常

### 规则管理
- [ ] 可以添加自定义规则
- [ ] 可以编辑规则
- [ ] 可以删除规则
- [ ] 可以启用/禁用规则

### 优先级
- [ ] 规则按优先级排序
- [ ] 低数字优先级优先匹配
- [ ] 自定义规则优先于过滤列表

### 自动更新
- [ ] 可以设置更新间隔
- [ ] 手动更新正常工作
- [ ] 更新进度正常显示

### 性能
- [ ] DNS查询响应快速（< 5ms）
- [ ] CPU使用率正常（< 10%）
- [ ] 内存使用正常（< 150MB）

## 🔄 停止服务

### Windows

按 `Ctrl+C` 停止程序

或者使用服务控制：
```cmd
AdGuardHome_v3_test.exe -s stop
```

## 📞 反馈

测试完成后，请记录：
1. 测试环境（操作系统、版本）
2. 测试结果（通过/失败的项目）
3. 发现的问题
4. 改进建议

---

**版本**: V3 Test Build  
**日期**: 2024-11-25  
**文档版本**: 1.0
